/* 全局样式 */
page { 
  box-sizing: border-box;
  background: #0a0a0a;
}

/* 微信小程序的 WXSS 在某些基础库版本中对通配符支持有限，避免直接使用 `*` */

/* 字体及通用容器 */
.container { padding: 24px; font-family: Helvetica, Arial, sans-serif; box-sizing: border-box; }
