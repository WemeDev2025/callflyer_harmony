<view class="sparkles" style="top:{{startTop}}rpx;">
  <block wx:for="{{sparks}}" wx:key="index">
    <view class="spark" style="left:{{item.x}}rpx;top:{{item.y}}rpx;animation-duration:{{item.d}}s;animation-delay:{{item.delay}}s;opacity:{{item.o}}"></view>
  </block>
  <slot></slot>
</view>
